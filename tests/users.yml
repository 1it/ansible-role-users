---
- hosts: localhost
  connection: local
  roles:
    - users

  vars:
    users_current:
      - username: 'sa'
        name: 'Test Admin'
        groups: ['admin']
        password: ''
        authorized: []
        state: 'present'

    users_group_list:
      - admin

